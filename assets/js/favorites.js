const L={ru:{home:'Главная',eye:'ВАШ СПИСОК',title:'Избранное',sub:'Сохранённые предложения остаются на этом устройстве.',fav:'Избранное',recent:'Недавно просмотренные',clear:'Очистить',empty:'Здесь пока пусто',emptySub:'Сохраняйте понравившиеся предложения сердечком на странице результатов.',search:'Перейти к поиску →',open:'Открыть',recentEmpty:'Вы ещё не открывали предложения.'},uk:{home:'Головна',eye:'ВАШ СПИСОК',title:'Обране',sub:'Збережені пропозиції залишаються на цьому пристрої.',fav:'Обране',recent:'Нещодавно переглянуті',clear:'Очистити',empty:'Тут поки порожньо',emptySub:'Зберігайте пропозиції сердечком на сторінці результатів.',search:'Перейти до пошуку →',open:'Відкрити',recentEmpty:'Ви ще не відкривали пропозиції.'},de:{home:'Startseite',eye:'IHRE LISTE',title:'Favoriten',sub:'Gespeicherte Angebote bleiben auf diesem Gerät.',fav:'Favoriten',recent:'Zuletzt angesehen',clear:'Löschen',empty:'Noch nichts gespeichert',emptySub:'Speichern Sie interessante Angebote auf der Ergebnisseite mit dem Herz.',search:'Zur Suche →',open:'Öffnen',recentEmpty:'Sie haben noch keine Angebote geöffnet.'},en:{home:'Home',eye:'YOUR LIST',title:'Favorites',sub:'Saved offers stay on this device.',fav:'Favorites',recent:'Recently viewed',clear:'Clear',empty:'Nothing here yet',emptySub:'Save interesting offers with the heart button on the results page.',search:'Go to search →',open:'Open',recentEmpty:'You have not opened any offers yet.'}};const lang=FBStorage.getItem('fb_lang')||'ru',t=L[lang]||L.ru;for(const [id,k] of [['homeLabel','home'],['eyebrow','eye'],['title','title'],['sub','sub'],['favTab','fav'],['recentTab','recent'],['clearAll','clear']])document.getElementById(id).textContent=t[k];document.documentElement.lang=lang;const $=id=>document.getElementById(id);function read(k){try{const value=JSON.parse(FBStorage.getItem(k)||'[]');return Array.isArray(value)?value.filter(x=>x&&typeof x==='object').slice(0,100):[]}catch{return[]}}function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function url(u){try{const x=new URL(u);return ['http:','https:'].includes(x.protocol)?x.href:'#'}catch{return'#'}}let tab='favorites';function card(x){return `<article class="item"><div class="pic">${x.image?`<img src="${esc(url(x.image))}" alt="${esc(x.title)}" loading="lazy" decoding="async">`:'🛍️'}</div><div class="body"><div class="source">${esc(x.source||'')}</div><div class="title">${esc(x.title)}</div><div class="meta">${esc([x.condition,x.delivery].filter(Boolean).join(' • '))}</div><div class="price">${esc(x.price||'—')}</div><div class="actions"><a class="open" href="${esc(url(x.url))}" target="_blank" rel="noopener noreferrer">${t.open} ↗</a>${tab==='favorites'?`<button class="remove" data-remove="${esc(x.id)}">♥</button>`:''}</div></div></article>`}function render(){const rows=tab==='favorites'?read('fb_favorites_v2'):read('fb_recently_viewed');if(!rows.length){$('content').innerHTML=`<div class="empty"><div class="heart">${tab==='favorites'?'♡':'↺'}</div><h2>${t.empty}</h2><p>${tab==='favorites'?t.emptySub:t.recentEmpty}</p><a href="index.html#home">${t.search}</a></div>`;return}$('content').innerHTML=`<div class="grid">${rows.map(card).join('')}</div>`;$('content').querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{FBStorage.setItem('fb_favorites_v2',JSON.stringify(read('fb_favorites_v2').filter(x=>x.id!==b.dataset.remove)));render()})}document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x===b));render()});$('clearAll').onclick=()=>{FBStorage.removeItem(tab==='favorites'?'fb_favorites_v2':'fb_recently_viewed');render()};$('theme').onclick=()=>{const n=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;FBStorage.setItem('fb_theme',n)};render();
