{
  "name": "findbest-ultimate",
  "version": "2.1.0",
  "private": true,
  "description": "FindBest.de smart product comparison with Telegram support and price alerts",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check.js",
    "test": "node --test test/*.test.js",
    "dev": "node scripts/dev.js",
    "build": "npm run check",
    "lint": "npm run check",
    "vercel-build": "echo \"No build step needed: static files under / and functions under /api are deployed as-is.\""
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
