{
  "name": "findbest-ultimate",
  "version": "2.1.0",
  "private": true,
  "description": "FindBest.de smart product comparison with Telegram support and price alerts",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check.js",
    "test": "node --test test/*.test.js",
    "dev": "node scripts/dev.js",
    "build": "echo \"No bundling required: static files under / and functions under /api deploy as-is. Run npm run check for local validation.\"",
    "lint": "npm run check"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
